services:
  aiconference:
    build: .
    container_name: aiconference
    restart: unless-stopped
    ports:
      - "127.0.0.1:8899:8899"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
      - ./knowledge_docs:/app/knowledge_docs
    env_file:
      - .env
